# Mobile App Environment Variables

# Authentication password (for app unlock)
# Default: diary123
# Production: Set to a secure password
MOOD_DIARY_PASSWORD=diary123

# JWT Secret Key (for token signing)
# Production: Use a long, random, secure key
JWT_SECRET_KEY=your_secret_key_here_change_in_production

# API Base URL
# Default: Uses platform-specific settings from config.ts
# Override only if needed
# API_BASE_URL=http://localhost:3001

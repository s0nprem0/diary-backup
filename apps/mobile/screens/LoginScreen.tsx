import React from 'react';
import { View } from 'react-native';

// Login screen removed — kept as a harmless placeholder to avoid import errors.
export default function LoginScreen() {
  return <View />;
}

